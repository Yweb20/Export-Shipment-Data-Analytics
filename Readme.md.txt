---

## 🐳 Docker Instructions

### Build the Docker Image
Open terminal inside the Docker folder and run:

docker build -t data_engineering_project .

### Run the Container

docker run data_engineering_project

### Output
The container executes the main Python script and generates the output files
inside the container or mounted directory as defined in the script.